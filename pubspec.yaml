name: awesome_input
description: This is a custom input package having multiple input types
version: 0.0.1

environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

library:
  name: lib/awesome_input.dart
